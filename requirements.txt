# businessfind runtime dependencies
#
# This project deliberately uses a SINGLE dependency. `.env` parsing is done
# with the standard library (config.py) — no extra package such as
# python-dotenv is added.
requests>=2.31.0
